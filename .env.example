# Discord Bot Configuration
DISCORD_TOKEN=your_discord_bot_token_here

# Optional: Set the command prefix (default is '!')
COMMAND_PREFIX=!

# Optional: Set debug mode (true/false)
DEBUG=false

# Optional: Postgres connection string for Railway
# Example: postgresql://user:pass@host:port/db
DATABASE_URL=

# Optional: separate dev token and environment selector
DISCORD_TOKEN_DEV=
ENVIRONMENT=production

# OpenAI configuration
OPENAI_API_KEY=
# Optional model override (defaults to gpt-4.5)
OPENAI_MODEL=
